#include <stdio.h>
#include "mesinkar2.h"
#include "bangunan.h"
int main(){
    START();
    // char C;
    // int N= CC;
    // ADV();
    // ADV();
    // int M= CC;
    // ADV());
    while (EOP!=-1){
        C= CC;
        if (CC=='\n'){
            printf("\n.\n");
        }else{
            printf("%c", CC);
        }
        ADV();
    }

    return 0;
}

